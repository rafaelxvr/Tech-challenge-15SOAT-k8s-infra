# Retired legacy Functions module

This module is preserved for historical migration and test inspection. It is not a deployable owner. The K8S repository environment roots under `infra/functions` are retired and fail closed before calling this module. New Lambda, authorizer, queue and DynamoDB ownership belongs exclusively to `oficina-functions/infra`.
